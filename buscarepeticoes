#include <stdio.h>
#include <stdlib.h>

int buscaSeTem(int n, int vet[n]){
    for(int i = 1; i < n; i++){
        if(vet[i] == vet[i-1]){
            return 1;
        }
    }
    return 0;
}

int main(){
    int n;
    scanf("%d", &n);
    int vet[n];
    for(int i = 0; i < n; i++){
        scanf("%d", &vet[i]);
    }
    int resposta = buscaSeTem(n, vet);
    if(resposta == 0){
        printf("Nao tem\n");
    }
    if(resposta == 1){
        printf("Tem\n");
    }
    return 0;
}
